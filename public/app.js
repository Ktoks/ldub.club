var ROOT_URL = 'http://localhost:8080';             // TESTING
// var ROOT_URL = 'https://ldubclub.herokuapp.com'  // ONLY IF DEPLOYING


var app = new Vue({
    el: '#app',
    data: {
        // this is where all of the data stuff goes

    },

    methods: {
        // methods
    },

    computed: {
        // if anything needs to be computed

    },
    // functions that run no matter what, such as the created function
    created: function() {
        console.log('Vue.js is ready. ')
    }
});